# 美珊與野狼碩班之旅 —— 功能規劃

建立日期：2026-07-27。供下一個 session 接手。

## 現況基準

| 項目 | 狀態 |
|---|---|
| 架構 | 純靜態 HTML + Firebase JS SDK（compat v8），GitHub Pages |
| 方案 | Firebase Spark 免費，**不升級 Blaze、不綁信用卡** |
| 安全規則 | 已部署 UID 白名單版（`firestore.rules`），2026-07-27 驗證通過 |
| 效能改造 | 階段 1–4 已完成但**尚未部署**：WebP 壓縮、索引/內文拆分、分頁、IndexedDB 快取 |
| 待辦 | Safari persistence 回歸測試、`migrate-diary.html` 遷移、照片儲存方案（Cloudinary vs ImageKit）未定 |

**動任何新功能前先把上面的待辦收掉**，否則會在移動的地基上蓋房子。

---

## A. 節日主題（使用者構想 1）

純前端，無成本、無後端、不碰 Firestore。可獨立實作。

### 架構
單一 `themes.js`，每個節日一筆設定：日期範圍 → CSS 變數覆寫 + 裝飾元素 + 可選動畫。前端在載入時比對今天日期套用。

```
{ id:'christmas', from:'12-20', to:'12-26',
  vars:{ '--accent':'#c1121f', '--bg-deco':'…' },
  deco:['tree','bell','reindeer'], particles:'snow' }
```

### 節日清單（台瑞雙邊，這是這個站的特色）
| 節日 | 日期 | 備註 |
|---|---|---|
| 聖誕節 | 12/20–12/26 | 鈴鐺、聖誕樹、馴鹿 |
| **Lucia 節** | 12/13 | 瑞典年度大節，燭光、白袍。去瑞典才懂的節日 |
| **仲夏節 Midsommar** | 6 月第三個週五 | 瑞典第二大節，花圈、五月柱 |
| 除夕／春節 | 農曆，需查表 | 紅色系、燈籠 |
| 兩人生日 | 固定日 | 各自不同主題 |
| 交往紀念日 | 固定日 | — |
| 抵瑞典週年 | 2027-09-XX | — |

### 實作注意
- 裝飾用 CSS 偽元素或絕對定位 div，**不要**塞進日記卡片，避免和已改好的分頁列表打架
- 粒子動畫（雪花）用 canvas，並尊重 `prefers-reduced-motion`
- 手機效能優先：粒子數量在小螢幕減半
- 主題設定寫死在前端常數，**不要進 Firestore**（不消耗配額）
- 兩人頁面同步套用（共用 `themes.js`）

---

## B. 文章摘要區（使用者構想 2）

### 不能做的
- ❌ 前端直接呼叫 Claude API — API key 會外洩
- ❌ 抓 NEJM / Economist 付費牆後的全文 — 違反服務條款
- ❌ 任何需要綁信用卡的方案 — 與既定前提衝突

### 建議架構：Claude Code 週更
```
每週手動或排程執行
  → 讀取 RSS（公開標題 + 摘要）
  → Claude Code 產出繁中摘要
  → 寫成 website/data/digest.json（進 repo）
  → 前端 fetch 該 JSON 顯示
```

零 API 成本、零 key 管理、不綁卡、不消耗 Firestore 配額。代價：不即時，週更。

### 可用來源（皆為公開 RSS，需先驗證各站 RSS 網址現況）
- NEJM：期刊目錄 RSS，含標題與摘要
- The Economist：分區 RSS
- 可考慮補：The Lancet、BMJ、KI 官方新聞（與學業直接相關）

### 顯示設計
卡片列表：標題、3–5 行繁中摘要、來源標記、發布日、原文連結。**必須明確標示摘要由 AI 生成**，避免日後誤引用。

---

## C. 其他建議方向（依投報率排序）

| 功能 | 價值 | 工作量 | 成本 | 說明 |
|---|---|---|---|---|
| **一年前的今天** | 高 | 小 | 0 | 查同月同日的舊日記推到首頁。日記網站的經典功能，情感價值最高、實作最便宜 |
| **雙時區時鐘 + 天氣** | 高 | 小 | 0 | 台北 / Solna 並排。Open-Meteo API 免費且**不需金鑰**，適合異地情境 |
| **心情趨勢圖** | 中高 | 小 | 0 | `mood` 欄位已存在，直接畫折線／熱力圖。零新資料 |
| **連續寫作 streak** | 中高 | 小 | 0 | 已有運動打卡的概念，套用到日記 |
| **PWA 化** | 高 | 中 | 0 | 可安裝、離線讀。你在 japan-trip 和 svenska-sprint 都做過，經驗可直接套 |
| **里程碑倒數** | 中 | 小 | 0 | 出發日、相見日、紀念日 |
| **年度回顧** | 高 | 中 | 0 | Spotify Wrapped 風格：今年寫幾篇、最常心情、照片牆。適合 12 月上線 |
| **全文搜尋** | 中 | 中 | 0 | 篇數過 300 後會需要。Firestore 無全文搜尋，且已分頁，需前端建索引或另存搜尋用精簡 collection |
| **匯出成書 / PDF** | 高 | 大 | 0 | 兩年後印實體書。這是整個站最有終點感的功能 |
| **對方發新日記推播** | 中 | 大 | 0 | 需 FCM + service worker，Spark 方案可用但複雜度高 |
| **地圖足跡** | 中 | 中 | 0 | 去過的地方標點。可用 Leaflet + OpenStreetMap，免金鑰 |

---

## D. 建議順序

1. **收掉現有待辦**（部署階段 1–4、Safari 測試、資料遷移、選定照片儲存）
2. **一年前的今天** + **心情趨勢圖** — 最便宜的情感回報，都用現有資料
3. **節日主題** — 使用者最想要，純前端無風險
4. **PWA 化** — 出國後網路不穩，離線讀取價值會突然變高
5. **雙時區 + 天氣** — 抵達瑞典後才真正有感
6. **文章摘要** — 需先決定 Claude Code 週更的執行方式
7. **年度回顧** — 對準 2026 年 12 月

## E. 全域原則

- 任何新功能都不得破壞已完成的分頁架構：**不要新增全量 `onSnapshot`**
- 新增 collection 必須同步更新 `firestore.rules`，否則讀寫會失敗（規則預設拒絕）
- 優先選「不需要金鑰、不需要綁卡、不需要後端」的外部 API
- 兩個頁面（wolf / meishan）的改動必須對稱
